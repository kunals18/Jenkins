@Library('kunals18/Jenkins') _

pipeline {
    agent any
    stages {
        stage('deploy kunal'){
            steps {
                createDir("kunal")
            }
        }
        stage('deploy app1'){
            steps {
                createDir("app1")
            }
        }
    }
}